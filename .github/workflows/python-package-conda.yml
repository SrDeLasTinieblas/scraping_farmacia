name: Python Package using Conda

on: [push]

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 5

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: '3.10'
    - name: Instalando los paquetes
      run: |
            pip install pipreqs
            pipreqs .
            ls -lah
            cat requirements.txt
            pip install -r requirements.txt
              
    - name: Ejecutando Scraping y subiendolo a la Base de datos
      run: |
            cd backend/scrapy/
            python __initTest__.py
      env:
        SQL_SERVER_CONNECTION_STRING: ${{ secrets.SQL_SERVER_CONNECTION_STRING }}

        #'DRIVER={{SQL Server}};SERVER={tinieblaserver.database.windows.net};DATABASE={testEmpresa};UID={FacturacionInventario};PWD={Darkangelo2023}'